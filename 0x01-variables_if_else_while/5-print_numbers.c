#include <stdio.h>
/* betty style doc for function main goes there */
/**
 * main - Entry point
 *
 * Return: this returns 0 by default
 */
int main(void)
{
	int p;

	for (p = 0; p < 10; p++)
		printf("%d", p);

	printf("\n");

	return (0);
}
