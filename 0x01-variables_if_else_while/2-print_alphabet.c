#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char simile;

	for (simile = 'a'; simile <= 'z' simile++)
		putchar(simile);
	putchar('\n');

	return (0);
}
