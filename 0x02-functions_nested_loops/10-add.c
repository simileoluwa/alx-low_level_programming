#include "main.h"
/**
 * add - adds two integer numbers and shows the result
 * @a: int added to b
 * @b: int added to a
 * Return: sum a and b
 */
int add(int a, int b)
{
	int c;

	c = a + b;

	return (c);
}
